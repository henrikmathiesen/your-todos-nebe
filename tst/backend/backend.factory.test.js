/// <reference path="../../typings/index.d.ts" />

// Todo
// Remember, can not include backend module (which depends on ngMockE2E) in a unit test
// need to mock that module some way and test the factory